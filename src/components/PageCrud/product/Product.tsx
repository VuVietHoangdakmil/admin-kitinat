import { PageCRUD } from "../../../types/enum";
type Props = {
  type: string;
};
const Product: React.FC<Props> = ({ type }) => {
  console.log("d", type);
  return <></>;
};
export default Product;
